[pytest]
testpaths = backend/tests
addopts = -q --strict-markers
markers =
    slow: needs the embedding and/or reranking model (first run downloads ~500MB)
# The tests import `backend.*`, so the repo root must be on sys.path.
pythonpath = .
filterwarnings =
    ignore::DeprecationWarning
